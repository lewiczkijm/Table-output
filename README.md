# Тестовое задание - таблица с данными

`npm install` — сборка.
`npm run dev` — сборка dev-версии
`npm run build` — сборка production-версии
`npm run server` — отдельный запуск сервера с данными
`npm run generate` — генерация тестовых данных
